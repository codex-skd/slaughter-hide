<h2>v0.0.0-beta.42 - Hanging carcass pose</h2>

<h3>Fix</h3>

<ul>
<li><strong>A carcass hung on a hook now stays hanging when it finishes draining</strong>, instead of snapping to a lying pose. It keeps the hanging shape through the whole cut-up (head, hide, cuts) until the last cut removes it.</li>
<li><strong>Taxidermy Table</strong>: restored the wide model (it was meant to be ~2 blocks wide). The block itself is fully solid; the overhanging half is still walk-through &mdash; making it solid needs turning the table into a real 2-block structure, which is a later change.</li>
</ul>
