<h2>v0.0.0-beta.36 - Farm animals tight scope (7 mobs)</h2>

<p>Tighter farm scope: only the 6 true farm animals + polar bear. Removes horse family and llamas.</p>

<h3>Changed (BREAKING)</h3>
<ul>
<li><strong>Scope cut from 13 to 7.</strong> Removed 6 families: horse, donkey, mule, and the 4 llama variants (brown/white/creamy/gray). Kept: cow, pig, sheep, chicken, rabbit, goat, plus polar bear (only non-farm mob requested to keep). Carcass definitions, blocks, items, recipes, head/mount/skeleton families and creative-tab entries removed. Skin set now 5 (cow/pig/sheep/goat/polar bear — chicken/rabbit have no skin item).</li>
<li>Lang trimmed 300 → 240 keys in en_us/es_es (60 orphan horse/llama keys removed).</li>
</ul>

<h3>Fix</h3>
<ul>
<li><strong>7-mob registry crash fixed.</strong> Carcasses.java static block and ModBlocks/ModItems registrations now consistent for 7 (previous beta.35 still referenced horse/donkey/mule RAW steaks in FOOD tab causing <code>cannot find symbol RAW_DONKEY_STEAK</code>).</li>
<li>Polar bear carcass correctly restored with its 6 voxel shapes (hanging/lying/head variants) — previous minimal add had duplicated shapes due to dead-code leftovers from 77-mob dump.</li>
</ul>
